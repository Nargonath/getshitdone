import styled from 'styled-components';

import { primaryColor } from 'styles';

export default styled.p`
  color: ${primaryColor};
  font-weight: 400;
  text-align: center;
  font-size: 3em;
  margin: 0;
`;
